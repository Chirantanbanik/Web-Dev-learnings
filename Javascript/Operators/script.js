const x = 4;
const y = 4;
const z = x === y;
console.log(z);
const a = x >= y;
console.log(a);

const firstNumber = true;
const secondNumber = false;
const result = firstNumber && secondNumber; // logical AND: if both operands are true
console.log(result)

const b = true;
const c = false;
const d = b || c;  // logical OR: true if either of the operand is  true
console.log(d);

// Logical NOT: true if operand is false and vide versa.